// Placeholder — app uses Firebase which requires a live environment.
// Integration tests should be run on a device/emulator with Firebase configured.
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('placeholder test', (WidgetTester tester) async {
    expect(true, isTrue);
  });
}
